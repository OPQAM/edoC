#include <stdio.h>

int main() {
    int a = 5;      // Declare an integer 'a' and initialize it with 5
    int b = 10;     // Declare an integer 'b' and initialize it with 10
    int sum = a + b; // Add 'a' and 'b' and store the result in 'sum'

    printf("Sum is: %d\n", sum); // Print the sum

    return 0; // Exit the program
}

