#include <stdio.h>

int main()
{	
	// ARRAYS
	
	
	int x[50];		/*an array of 50 integers, called 'x'*/
	
	x[0] = 5;
	x[3] = 500;

	printf("%i, %i\n", x[0], x[3]); /*note that the last element of the array is x[49]*/
	}
